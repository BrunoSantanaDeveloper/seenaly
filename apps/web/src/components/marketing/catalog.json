{
  "_readme": "Machine-readable catalog of the marketing component library (the Lovable design-system.json idea). The PreToolUse marketing-guard injects a digest of this file into every write/edit under app/(marketing)/** or components/marketing/**, so the full expressive range is in context on every generation — not just the archetypes the model happens to remember. Keep entries in sync when a component's props change; guard lines render as `Name(props) — use`.",
  "contract": [
    "Every content block sits in <Section>/<Container>/<SectionHeader> — spacing/width come from marketing tokens, never per-page values",
    "Tokens only: hsl(var(--token)) / token Tailwind classes; must look right in 4 color themes × light/dark",
    "Headings: font-display text-display-{2xl,xl,lg,md} (fluid clamp scale, no breakpoint font jumps)",
    "Copy through the marketing i18n namespace in ALL 5 locales (de,en,es,fr,pt-BR)",
    "Icons only via @/icons/nexture/ni-* alias; meaningful concept icons, never decoration",
    "Exactly one <h1> per page (Hero, or SectionHeader/PricingSection as/headingAs=\"h1\")",
    "New public routes: PUBLIC_PREFIXES (src/middleware.ts) + src/app/sitemap.ts + generateMetadata",
    "Structured data via <JsonLd> only; Organization/FAQPage/Product already emitted by layout/Faq/PricingSection",
    "Motion via <Reveal> or useGSAP in client components; transforms + autoAlpha; reduced-motion safe"
  ],
  "tones": {
    "source": "tone.ts — TONE map + toneAt(index) rotation",
    "values": ["primary", "secondary", "accent-1", "accent-2", "accent-3", "accent-4"],
    "rule": "primary is reserved for CTAs and the page's one bold moment; categorical elements (plan tiers, feature families, chart series, icon chips) take accent hues with a CONSISTENT family→hue mapping across the whole site. A 100%-primary page reads monochrome and fails the premium bar."
  },
  "components": [
    {
      "name": "Section",
      "file": "section.tsx",
      "role": "layout",
      "props": "spacing: default|compact, background: default|paper|primary-soft|contrast, decor: none|glow|grid|gradient-edge, bleed, id",
      "use": "vertical rhythm unit wrapping every block; vary background/decor at least twice along a page (premium bar #3)"
    },
    {
      "name": "Container",
      "file": "container.tsx",
      "role": "layout",
      "props": "className",
      "use": "token width clamp; Section renders it — direct use only with Section bleed"
    },
    {
      "name": "SectionHeader",
      "file": "section-header.tsx",
      "role": "layout",
      "props": "eyebrow, title, subtitle, align: start|center, as: h1|h2",
      "use": "standard section heading; as=\"h1\" on the first header of hero-less pages"
    },
    {
      "name": "Hero",
      "file": "hero.tsx",
      "funnel": "attention + value proposition",
      "props": "layout: center|split, decor: glow|grid|none, media (evidence slot), eyebrow, title, subtitle, primaryCta, secondaryCta",
      "use": "owns the page <h1> AND the page's one orchestrated motion moment (built-in load timeline: copy staggers, media rises); split + <ProductFrame glow> is the committed direction for product-heavy pages; media is required in practice (premium bar #1)"
    },
    {
      "name": "ProductFrame",
      "file": "product-frame.tsx",
      "role": "evidence",
      "props": "glow, children (real screenshot via next/image)",
      "use": "browser chrome for REAL product screenshots; token wireframe placeholder without children; glow = primary halo, the expensive-product hero treatment"
    },
    {
      "name": "DataVizPlaceholder",
      "file": "data-viz-placeholder.tsx",
      "role": "evidence",
      "props": "label",
      "use": "animated token-driven chart standing in for the screenshot on DATA products; CSS draw-in, reduced-motion safe"
    },
    {
      "name": "FeatureRows",
      "file": "feature-row.tsx",
      "funnel": "desire (depth)",
      "props": "items: {eyebrow?, title, body, bullets?, media, tone}, decor, id",
      "use": "alternating text↔evidence zig-zag for the 2–4 flagship features; use INSTEAD of a second card grid; decor=\"grid\" makes it the page's technical section"
    },
    {
      "name": "BentoGrid",
      "file": "bento-grid.tsx",
      "funnel": "desire (secondary)",
      "props": "items: {title, body, visual?, featured?, tone} — 4–6 cells",
      "use": "asymmetric grid; featured cells span 2 columns and carry a visual; breaks equal-card monotony"
    },
    {
      "name": "StatBand",
      "file": "stat-band.tsx",
      "funnel": "trust (proof)",
      "props": "stats: {value (pre-formatted), label, tone} — 3–4 stats",
      "use": "contrast band of oversized REAL numbers; doubles as a background break; invented stats are worse than none"
    },
    {
      "name": "ProcessSteps",
      "file": "process-steps.tsx",
      "funnel": "desire (how it works)",
      "props": "steps: {icon, title, body, deliverableLabel?, deliverableValue?, tone}",
      "use": "scannable sequence: ghosted ordinal + meaningful themed icon + optional deliverable, one hue per step"
    },
    {
      "name": "FeatureGrid",
      "file": "feature-grid.tsx",
      "funnel": "desire",
      "props": "features: {icon, title, description, tone}",
      "use": "benefit-led card grid, meaningful icon per family hue; never the only archetype on a page (premium bar #2)"
    },
    {
      "name": "Testimonials",
      "file": "testimonials.tsx",
      "funnel": "trust",
      "props": "items: {quote, name, role, tone?}",
      "use": "quotes must carry a concrete outcome (numbers, before/after); initials avatars rotate harmonic hues, no stock photos"
    },
    {
      "name": "LogoCloud",
      "file": "logo-cloud.tsx",
      "funnel": "trust",
      "props": "label, items: {name, logo?}",
      "use": "client strip; muted wordmarks until real token-tinted SVG logos exist"
    },
    {
      "name": "PricingSection",
      "file": "pricing-section.tsx",
      "funnel": "action",
      "props": "plans (from getDisplayPlans()), ctaLabel, ctaHref, headingAs: h1|h2, decor, id",
      "use": "real plans, one highlighted anchor, tone per tier; emits Product JsonLd; CTA repeats the page's primary action verbatim; decor=\"glow\" when it leads the page"
    },
    {
      "name": "Faq",
      "file": "faq.tsx",
      "funnel": "objection handling",
      "props": "items: {question, answer}",
      "use": "REAL purchase objections (price, lock-in, security, migration) answered plainly; emits FAQPage JsonLd"
    },
    {
      "name": "Cta",
      "file": "cta.tsx",
      "funnel": "recovery",
      "props": "title, subtitle, cta",
      "use": "last conversion opportunity; repeats the primary CTA verbatim"
    },
    {
      "name": "Reveal",
      "file": "reveal.tsx",
      "role": "motion",
      "props": "stagger, delay, className",
      "use": "the ONLY sanctioned scroll animation (transforms + autoAlpha, plays once, reduced-motion static); do NOT wrap every section uniformly — one orchestrated GSAP moment per page (premium bar #5)"
    },
    {
      "name": "JsonLd",
      "file": "json-ld.tsx",
      "role": "seo",
      "props": "data (schema.org object)",
      "use": "the only way to emit structured data; never hand-write an ld+json <script>"
    },
    {
      "name": "Prose",
      "file": "prose.tsx",
      "role": "layout",
      "props": "className, children",
      "use": "long-form reading rhythm (legal pages, articles) without styling each element by hand"
    }
  ]
}

// Importing the platform modules here registers their brief providers as a
// side effect (the same pattern as lib/connectors.ts). Add future platforms
// (Google Ads, TikTok Ads) alongside meta.
import "./meta";

export * from "./registry";

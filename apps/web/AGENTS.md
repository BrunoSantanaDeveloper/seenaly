# Codex web context

Read `../../AGENTS.md`, `../../CLAUDE.md`, and this directory's `CLAUDE.md`
before working in `apps/web`. The root `AGENTS.md` maps target paths to the
canonical rules and mandatory skills under `.claude/`.

Do not duplicate those rules here. Update the canonical document or rule so
Claude and Codex receive the same change.
